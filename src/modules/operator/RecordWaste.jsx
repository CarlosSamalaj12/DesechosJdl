// DEPRECADO — el flujo del operador ahora se hace desde OperatorHome
// con un tap por categoría y modal. Este archivo se mantiene solo para
// no romper imports viejos. NO USAR.
export default function RecordWaste() { return null; }
